# Adversarial review 5 — 2026-07-28

Scope: `PAPER.md` read front to back as a paper; `docs/paper.html`; `README.md` and
`docs/index.html` against the filesystem and the ledger; all 115 internal
cross-references parsed and resolved by script; `evidence/check-corrections.py`
attacked afresh on two copies of the tree; `evidence/sync-ledger.py` and
`evidence/build-paper.py` read as the sources of every published count;
`survey/31` read against `research/raw/H2-selpa-practitioner-reality.md`.

Reviewed at commit **`e1ed691`**. Nothing in `/home/eranti/learning-with-ai` was
modified by this review; all attacks were run on copies in scratch.

---

## VERDICT

**Publish — after one build. Not "not yet."**

The argument is publishable and I would defend it in front of a hostile referee today.
Three of the four things I blocked on in round 4 were answered properly: 92 of the 115
cross-references now land on the section they name, the agentic rule is correctly
stated as a bound in the section that carries it, and §23 now opens on a week a
coordinator would recognise. That is real work and it is done.

I am not withholding a fifth time. No remaining defect requires research and none
changes a conclusion. But I will not repeat round 4's line that the blockers are only
in the packaging: **this round I also found nine local errors in the body**, and three
of them are the paper committing a sin it names elsewhere. They are listed and verified
below. The reason they do not block is that this document's stated claim is not *"we
got everything right"* — `README.md:103` says the opposite — so the correct disposition
is nine new ledger rows, not a fifth refusal.

Four things must be fixed before anyone is invited to read it. Two are **false
statements in the abstract**: 

1. **The paper's honesty number is wrong, and wrong in three mutually inconsistent
   machine-generated forms.** `PAPER.md:22–23` says *"**17 of the 37** corrections were
   found by an adversarial reviewer"*. `README.md:109` says **16 of 37**.
   `CORRECTIONS.md:85` — the ledger the abstract is pointing at — says **"15 of 36"**.
   The true answer is **16 of 37**. Fourth consecutive round on this number, twice
   logged as corrected. Cause below; the fix is two lines.
2. **The abstract states the paper's organising rule in the exact form C-37 records as
   corrected.** `PAPER.md:27` — *"a rule: the value of an agentic loop **equals** the
   value of the external check it closes on"* — against `PAPER.md:1547`, *"is **bounded
   by**"*. `PAPER.md:30` files SciCode under *"Where none exists"*; `PAPER.md:1567` says
   *"SciCode is not check-free — it has hand-written tests."* `README.md:20` and `:24`
   carry the superseded form entire. The correction was applied to one of three
   surfaces and the two it missed are the two a reader meets first.

Both are one line in `evidence/build-paper.py`. Neither survives a rebuild. But **do
not announce this paper anywhere until they are fixed**, because a referee who does
exactly what `README.md:113` invites — *"read those before you read the survey"* —
opens `CORRECTIONS.md`, reads *"15 of 36"*, turns to the abstract, reads *"17 of the
37"*, and has found the survey that cannot count its own errors before reaching §1.

The other two are in the body, and they are the two a hostile referee would quote:

3. **`PAPER.md:8327` — *"One hundred and twenty-eight notebooks. Zero exercises. That
   is not an impression. It is a programmatic scan of every markdown cell in **three
   sampled** zero-to-hero notebooks."*** The table underneath has three columns. A
   3-of-128 sample stated as a universal zero, in the section that reads other people's
   builds as evidence, in a paper whose §4 commitment is *"Publish the denominator."*
   **Fix: "Three notebooks, scanned exhaustively. Zero exercises," and label the
   128-wide version `INFERENCE`.** (The separate 128-wide claim at `:8560` and `:8627`
   — *"nine deployed apps and 128 notebooks"*, zero **outcome measurements** — is a
   different assertion and I am not disputing it.)
4. **`PAPER.md:7323` — *"Model capability does **not** transfer to tutoring capability,
   and there is now a number. Solving ability and pedagogical ability correlate at
   **r = 0.421**."*** r = 0.421 is a moderate *positive* correlation; it is evidence of
   partial transfer, and it contradicts the sentence it is offered to prove. This is a
   number cited against its own meaning, in a survey that exists to catch exactly that.
   **Fix: "transfers only weakly — r = 0.421, so solving skill explains under a fifth of
   the variance in pedagogical skill."**

Everything else below is a proof-stage list.

---

## THE FIVE FIXES, VERIFIED OR NOT

### 1. C-34, the cross-references — **substantially fixed; one class newly broken**

**The claim checks out for what it covers.** I parsed `PAPER.md` myself: 123 `§NN`
tokens, of which 7 are `§300.xxx` CFR citations and 1 is `§609`, leaving **115 internal
references, all of which resolve to a section numbered 1–33.** No dead references. The
remap is correct where I spot-checked it against sources: `survey/24:213`'s *"§19 and
§03"* renders as *"§28 and §8"* (The Canon; Ekalavya's Thumb) ✅;
`survey/26:237`'s *"§14 and §17"* renders as *"§31 and §19"* (Motivation; Showing) ✅;
`survey/27:103`'s *"§23"* renders as *"§4"* ✅; the two that mattered most in round 4 —
`PAPER.md:204–205` and `PAPER.md:1646–1647` — now read *"§16"* and *"§10"*, which are
the right doors. **83-of-87-wrong is now 92-of-115-right.**

**Now the subtler question you asked me to check, and the answer is not clean.**

`renumber()` at `evidence/build-paper.py:168` is `re.sub(r"§\s?(\d{2})\b", sub, text)`.
It matches **exactly two digits**. That is correct and deliberate — it is what keeps
`§300.323(d)(2)` and `§609` safe. But the survey sources use **two conventions**, and
only one of them is two digits:

- `§04`, `§25`, `§09` — zero-padded, means *another survey file*. Remapped. Correct.
- `§4`, `§2`, `§3.1` — unpadded, means *a numbered subsection of the file you are
  reading*. **Not remapped, not reported, and now colliding with real paper sections.**

There are **23 of these**, and they are the failure mode you named: a reference that
lands on a real but wrong section.

| PAPER.md | in section | prints | means | lands on |
|---|---|---|---|---|
| 1147, 1197, 1288 | §5 JARVIS | `§4` ×3 | *"the refusal engine"*, its own subsection 4 (`PAPER.md:1168`) | **§4 Fifteen Hundred Papers, Seven Trials** |
| 1238 | §5 | `### Revisions to §2 and §3` | its own subsections 2 and 3 | **§2 What Works, §3 The Scoreboard** |
| 1280 | §5 | `### Revised build order (supersedes §5)` | its own subsection 5 | **§5 — the section it is inside** |
| 2688 | §11 Archivist | *"The reason that is good news arrives in §4"* | its own subsection 4 (`:2780`) | **§4** |
| 1685 | §7 Pytest | *"is §6's unclaimed prize"* | its own subsection 6 (`:1655`) | **§6 The Compression** |
| 761 | §3 Scoreboard | *"Every positive result in §1"* | its own subsection 1, *"The band"* | **§1 The Floor** |
| 3826, 3847, 3893 | §15 | `§3.1`, `§3.2`, `§3.1` | subsections of §15 | **nothing — §3.1 is not a thing** |
| 4416 | §17 | *"it is why §2.2 exists"* | subsection of §17 | **nothing** |
| 3697 | §15 | *"see provenance note in §1"* | its own §1 | **§1 The Floor** |
| 6435 | §23 | *"And one more, from §3"* | its own §3, the PWN correction | **§3 The Scoreboard** |
| + 5498 (§20), 6638 (§24), 6774 (§25), 8274 (§29), 8986 (§31), 3628 (§14), 2599 (§10), 3292 (§13), 4163 (§16) | | `§1`/`§2`/`§3`/`§4` | own subsections | real, wrong sections |

The three in §5 are the worst, because they sit inside a **bolded numbered build
order** — *"2. **The refusal engine (§4).**"* — which is exactly where a reader
follows a pointer.

**And a second, smaller class: the remap is faithful to references that were already
pointing at the wrong section.** `renumber()` validates the *scheme*, never the
*target*, so a source reference that was wrong stays wrong with a new number.
Two I can demonstrate:

- `PAPER.md:4331` — *"**78.5%** [77.2, 79.8] persistence once it happens — figures §12
  works through."* `78.5` occurs exactly twice in the paper: here, and at `:8544` as a
  file size in KB. **§12 does not contain the figure.** Source: `survey/26:143`.
- `PAPER.md:8798` — *"Bai, Hew & Huang 2020 … **g = 0.504** [0.284, 0.723] (§1)"*.
  `survey/14:160` wrote `(§24)`, correctly remapped to §1 — and **§1 The Floor contains
  no gamification result at all**; `grep -n 'Bai\|gamif' survey/24-the-floor.md` returns
  nothing.

So the answer to your question is: the 92 two-digit references resolve to the right
section in every case I sampled *except* where the source was already pointing
somewhere the figure does not live, which the build cannot detect and did not.
**Fix: after remapping, assert that each target section's text contains a token from
the sentence making the reference — or, more cheaply, print every reference with its
target title so a human can read 115 lines once.**

**Fix (one line, in the pass that already exists):** inside `renumber()`, after the
two-digit substitution, run `re.sub(r"§(\d)(\.\d+)?\b", lambda m: f"§{n}.{m.group(1)}{m.group(2) or ''}", text)`
where `n` is the paper number just assigned in pass 1 — turning `§4` into `§5.4` and
`§3.1` into `§15.3.1`. Then extend the `unresolved` report to single-digit refs so the
build tells you rather than you finding out here. **And do not ship the current state:
`§4` printed three times inside §5 is worse than the dead link it replaced, because
the reader who follows it arrives somewhere plausible.**

### 2. C-35, the counts — **mechanism fixed, arithmetic wrong, three ways**

Verified against the filesystem: `ls research/raw/*.md` → **35**, and `PAPER.md:5` says
35 ✅. `grep -c '^| \*\*C-'  CORRECTIONS.md` → **37**, and the head says 37 ✅.
Sections 33 ✅, words 80,725 ✅ (my independent split gives 82,728 including front
matter — consistent). Everything is derived. **And the one number that carries the
paper's ethical claim is wrong anyway.**

`evidence/build-paper.py` computes the external count **twice, differently, in the same
file**:

```
:65   N_EXT = len(re.findall(r"\| \*\*EXTERNAL-REVIEW\*\* \|", ledger))     → 17   → the ABSTRACT
:611  counts["external"] = sum("EXTERNAL-REVIEW" in r for r in rows)        → 16   → README + dashboard
```

Line 65 scans the whole file, so it counts **`CORRECTIONS.md:24`** —
`| **EXTERNAL-REVIEW** | An adversarial reviewer found it. We did not. |` — the
**legend row that defines the label** — as an instance of the label. Line 611 counts
only inside parsed `| **C-nn** |` rows and gets it right.

`evidence/sync-ledger.py` computes it a **third** way. Its row regex is
`^\| \*\*(C-\d+)\*\* \|(?:[^|]*\|){3}\s*([^|]+?)\s*\|$` — five cells, no pipes inside
them. **C-36's row contains `` `\| C-99 \|` `` in its own text**, so the row does not
parse and is silently dropped. I re-ran the script on a copy: it reproduces the
committed scoreboard byte-for-byte, so this is not staleness — it is structural.
`total=36, external=15`. And like the checker's `if n_surfaces < 20`, its only guard is
`if not rows: refuse to write` — it protects against total failure, never against
losing one row.

So the same fact is published as **17 of 37** (`PAPER.md:22`), **16 of 37**
(`README.md:109`, `docs/index.html`), and **15 of 36** (`CORRECTIONS.md:85`). Truth:
**16 of 37**.

**Fix:** delete `N_EXT` at `:65` and reuse the `:611` computation for both; in
`sync-ledger.py` replace the five-cell regex with `^\| \*\*(C-\d+)\*\* \|(.*)\|$` and
split on unescaped pipes, or simply assert `len(rows) == len(re.findall(r'^\| \*\*C-\d+\*\*', t, re.M))`
and exit non-zero on mismatch. Same assertion in `build-paper.py`.

**Two smaller things the same fix should sweep up.** `PAPER.md:19–20` now reads *"It
rests on 35 research\nreports;␣␣every claim carries an evidence label,"* — the citation
figure was deleted rather than computed, leaving a double space and a dangling
semicolon in the abstract's second sentence. Meanwhile `README.md:6`, `README.md:141`
and `docs/index.html:106` still hand-carry *"~2,100 sources"*, which is the number the
abstract was corrected for. And `README.md:113` — *"The three review reports are in
`evidence/`"* — there are four.

### 3. C-36, the checker — **four holes closed, the fifth is that it does not check the corrections.** Full attack below.

### 4. C-37, the agentic rule — **fixed in the section, not in the abstract or README, and the fix has left a contradiction next to it**

`PAPER.md:1547` now reads *"The value of an agentic loop **is bounded by** the value of
the external check it closes on"*, `:1564` restores `| SciCode | **4.6%** | Weakly —
hand-written tests |`, and `:1567–1571` makes the reasoning explicit: *"SciCode is not
check-free … which is why the rule is stated as a bound rather than an equality. A weak
check bounds you low. A strong one does not guarantee you reach the bound."* That is
the right correction, better argued than I asked for, and it survives PaperQA2 in a way
the old form did not. **In §7, the rule is now correctly stated.**

It did not propagate. `PAPER.md:27` and `README.md:20` still print *"equals"*;
`PAPER.md:30` and `README.md:23–24` still file SciCode under *"Where none exists"* and
still assert *"the axis is not difficulty"*.

**And the fix has created a live contradiction inside §7 itself.** `PAPER.md:1566`:
*"A twenty-fold spread, and the axis is not how hard the task is."* One sentence later
the table concedes SciCode **has** a check and still scores 4.6%. If check-quality is
the axis, SciCode is unexplained; the section rescues it with *"a weak check bounds you
low"* and offers **no evidence that SciCode's hand-written tests are weak** rather than
that novel scientific code is hard. Round 4 asked for one sentence conceding that
difficulty and checkability are confounded across these four rows — SWE-bench
*Verified* is the human-filtered solvable subset, PaperBench is *replicate an ICML
Spotlight*. That sentence is still missing, and the honest table has made its absence
sharper, not softer.

**Fix:** three edits. `build-paper.py:27` *equals* → *is bounded by*, and drop SciCode
from the *"where none exists"* clause; same in `README.md:20–24`; and after
`PAPER.md:1566` add *"Difficulty and checkability are confounded across these four
rows — SWE-bench Verified is the solvable subset, PaperBench is a full replication —
so read the gradient as a bound, not a decomposition."*

**Two supporting overstatements in §7, both named in round 4, both untouched.**
`PAPER.md:1600` — *"Selection by LLM judge **measures** −3.20pp. Selection by test
**measures** +8.14pp"* — and again as a bolded commitment at `:1712`, *"Never select by
LLM judge. −3.20pp against +8.14pp."* `K2:1092–1093` labels that pair `INTERNAL-PRIOR`:
it is this project's own §20, stated twice as an external measurement.
`PAPER.md:1629` — *"Explanatory animation renders at 93.8%. The visual half of §19's
argument has a working pipeline"* — 93.8% is TheoremExplainAgent's **success rate**;
`K2:210–215` gives an **overall score of 0.77** and the authors' own caveat about layout
defects. It is a render rate quoted as a capability, pointed at the one section whose
thesis is the arithmetic of a wrong picture. This is C-14 with the nouns changed, third
round.

### 5. §23's week — **verified.** See below.

---

## MY ATTACK ON THE CHECKER

Baseline on a clean copy: `self-test: OK — all 14 rules fire on their planted probe` /
`OK — 14 rules, 53 published surfaces, 37 ledger ids, 0 violations`, rc 0.

**The four named holes are genuinely closed.** I replayed all four: an invented
`| C-99 |` id no longer claims the exemption; `PAPER.md` and `docs/paper.html` are in
`SURFACES`; C-16 now fires on *"Retrieval practice g = 0.50 across 222 classroom
studies"*; and `rm -rf` of everything now fails on the surface floor. Good.

**Then I planted 16 fresh evasions plus one control. The control was caught. Two
evasions were caught. Fourteen were not.** Three structural attacks on top of that,
all three successful.

| # | Evasion (planted in `survey/_attack.md`) | Rule | Caught |
|---|---|---|---|
| 0 | *"Nickow et al. pooled tutoring effect is 0.37 sd"* (control) | C-6/C-12 | ✅ |
| 1 | `\| **C-13** \| Nickow … 0.37 sd, … d = 0.971, Kestin 0.63-0.73, and this is the strongest evidence in the history of educational technology. \|` | four rules | ❌ |
| 2 | `0.63‑0.73` (U+2011 non-breaking hyphen — visually identical) | C-13 | ❌ |
| 3 | `0.63−0.73` (U+2212 minus) | C-13 | ❌ |
| 4 | *"a pooled tutoring effect of **0.370** sd"* | C-6/C-12 | ❌ |
| 5 | *"the interaction is d = **0.9710**"* | C-4 | ❌ |
| 6 | `0.3<U+200B>7` (zero-width space inside the number) | C-6/C-12 | ❌ |
| 7 | *"…strongest evidence in the history of educational technology. The difference in banner colour was **not significant**."* | C-1 | ❌ |
| 8 | Orton-Gillingham overclaim followed by the paper's own `## The nulls, given their own space` heading | C-3 | ❌ |
| 9 | *"A crew of **7**, each narrow"* | C-19 | ❌ |
| 10 | *"+127% with **no penalty on retention**"* | C-2 | ✅ |
| 11 | *"Cepeda's meta‑analytic tradition"* (U+2011) | C-15 | ❌ |
| 12 | *"Item generation for the adversary **is unbounded**"* | C-20 | ❌ |
| 13 | 99.1% and 0.61 ms separated by 68 characters | C-21 | ❌ |
| 14 | *"g = 0.50 across 222 **separate** classroom studies"* | C-16 | ❌ |
| 15 | *"The teachable-agent effect is g = 0.56…"* | C-7 | ✅ |
| 16 | Sierra Leone and *"school year"* separated by 130 characters | C-22 | ❌ |

Structural, each on its own copy:

- **One invalid byte deletes a file from the scan and the file is still counted.**
  I put a single `\xff` into `survey/09-the-scoreboard.md` and appended three retired
  values to it. `except Exception: continue` (`:142`) drops the file; `surfaces()`
  never reads, so the count is unchanged. Output: *"OK — 14 rules, **53** published
  surfaces, 37 ledger ids, 0 violations"*, rc 0. Named in round 4's fix list; not done.
- **Bare `--self-test` still returns before scanning.** `:179–180`. With three retired
  values live in `README.md`, the command the file's own usage block documents at `:31`
  prints *"self-test: OK — all 14 rules fire on their planted probe"* and exits **0**.
  The same tree under `--strict` exits 1. Named in round 4; not done.
- **`if n_surfaces < 20` guards the shape of my last demonstration, not the failure.**
  I deleted `README.md`, `PAPER.md`, `docs/paper.html`, `docs/index.html`,
  `process/CLAUDE.md` and `process/AUDIT.md` — **every named, non-glob surface, the
  entire deliverable** — and the 33 survey files plus 13 demos hold the count at **47**.
  `--self-test --strict` prints *"OK — 47 published surfaces … 0 violations"*, rc **0**.
  The floor is set at 20 against a real count of 53; it catches `rm -rf` and nothing
  narrower. **Assert that every literal (non-glob) entry in `SURFACES` exists, and that
  every glob returns a nonzero count.**

### The fifth hole

Not any of those. **The checker does not check the corrections.**

`RULES` covers C-1, C-2, C-3, C-4, C-6/C-12, C-7, C-13, C-15, C-16, C-18, C-19, C-20,
C-21, C-22. **Fourteen rules. The highest id is C-22. The ledger has thirty-seven rows.**
Every correction from round 2 onward — C-23 through C-37, fifteen of them, including
every one of the four blockers this round was meant to answer — has **no propagation
rule at all**. The success line says *"14 rules … 37 ledger ids"* and prints both
numbers next to each other without noticing.

That is not academic. **The live violation in this repository right now is C-37**, whose
superseded form sits in `PAPER.md:27` and `README.md:20`, and the checker prints OK
because no rule exists for it. The same is true of C-35: the count defect is a
correction that was logged and then re-broken, and nothing looks for it. The tool built
to stop a correction from surviving in one surface while being fixed in another is
blind to the last fifteen corrections, which is to say blind to every correction an
adversarial reviewer has ever produced.

**Fix, and it is the only one that changes the class rather than the instance:** in
`check-corrections.py`, parse `CORRECTIONS.md` for the set of ids, and **fail if any id
has no rule and no explicit `NO_RULE = {"C-24": "process finding, nothing to propagate"}`
waiver with a written reason.** Then adding a ledger row forces a decision instead of
silently widening the gap. Second: give every regex *branch* its own probe, not every
rule (`--self-test` currently plants one string per rule and certifies multi-alternative
patterns it never exercises). Third: normalise Unicode dashes and strip zero-width
characters before matching. Fourth: make `--self-test` always scan. Fifth: **run it.**
There is no hook, no CI, and `.autosave.sh` still pushes every 180 seconds without it.

A last note, minor: `evidence/check-repetition.py:20` accepts `§\s?\d{1,2}` anywhere
within 700 characters as proof that a restatement is cross-referenced. It does not
check the *target*. It currently certifies *"every restatement cross-referenced"* on a
document containing 23 references to the wrong section.

---

## DOES A COORDINATOR RECOGNISE THE WEEK

**Yes. Settled — this is fixed, and I am closing it.**

I raised this twice and the answer was twice *"the section is the answer to a different
question."* It is no longer. `survey/31:15` now reads **"So start with the week"**, and
what follows is a week:

- **The budget, stated first**: *"administrative work runs a median of **five hours a
  week against four available**. The deficit is structural. It does not clear on
  Friday."* That is the sentence the section was missing. It is the practitioner's
  actual complaint, in the practitioner's actual unit.
- **Standing weekly**: direct instruction, consultation with every general-education
  teacher under §300.323(d)(2) — *"which makes **every new teacher, every schedule
  change, and every substitute a re-notification event**"*. That reframing is new, it is
  correct, and it is the sort of thing only someone who read the regulation would find.
- **Rolling**: 1.5 hours in an IEP meeting and 2 hours to write the document; 2 hours a
  month scheduling; **8 hours of progress reports every 7 weeks**; 7.5 hours a month
  administering initial evaluations and 4.2 reviewing; behaviour logs 5, BIPs 2, FBAs 2.
- **The compliance calendar**, six clocks with dates: 60 days consent→evaluation,
  30 days eligibility→IEP, an IEP in effect on day one, annual review, triennial
  reevaluation, **ten school days to a manifestation determination**.
- **And the part no calendar holds**: a safeguarding disclosure, *"personal duty,
  immediate, routed around the parent"*; an IEE request that *"starts a clock the moment
  it is spoken"*; a transfer student owed comparable services immediately.

Round 4 asked for a caseload number, the annual-review and triennial clocks, and one
referral-to-signature sequence. Three of four are there: the clocks, and 60→30→signature
is the sequence. The closing line — *"The paperwork is the job's second full-time role,
and it is the one with the legal exposure"* — is the correct verdict on a week and it is
earned by the fifty lines above it.

**Two things left, both small, both in the source report already.**

- **The caseload number, which is the one a coordinator would give you first.**
  `H2:1375` heads its own paragraph *"The caseload arithmetic, **which is the number to
  carry**"*: SPeNSE's **0.6 hours/week for each child you case-manage**, against
  California Education Code **§56362(c)**'s verbatim cap — *"A resource specialist shall
  not have a caseload that exceeds 28 pupils"* — gives **16.8 hours/week of
  administrative work at a legal maximum caseload, against 4 available**. And the
  conclusion `H2:1392–1394` draws from it is the best sentence in the report:
  *"the SPeNSE mean of 5–6 hours/week is therefore not a measure of the work required;
  **it is a measure of how much of the required work gets done.**"* §23 publishes the
  5-against-4 and omits both. Without an N, every per-unit figure in §1 — per IEP, per
  7 weeks, per month — has nothing to multiply by. **Fix: three sentences from
  `H2:1375–1394`, carrying H2's own `INFERENCE` label.** It makes the section's point
  four times harder and converts *"the deficit is structural"* from an assertion into
  arithmetic.
- **No vintage on the hour counts.** Every time-use number is SPeNSE, and `H2:2890`
  records *"no newer national time-use study was located; the best available remains
  2002"*, alongside ED's own admission that *"we lack detailed data on the number of IEP
  Team meetings conducted each year."* A week built on 2002 data is still the best week
  available — but the paper's discipline is to say so. **Fix: "(SPeNSE, 2002 — the most
  recent national time-use study; ED, 71 FR 46845, records that no better data exists)"**
  once, under the heading.

And one line is now falsified by its own section: `survey/31:253` still closes *"This
section is the answer to **a different question**"*. It was, in round 4. It is not now.
**Fix: "§22 asked what the evidence says a system should teach. This section asks what
it is allowed to touch, and who signs."**

---

## READING IT AS A PAPER

**It reads as a paper. That is a change in kind, not degree, from round 4.** The spine
holds: evidence → what the evidence forces → the mechanisms → how to be wrong safely →
who it is for → what already exists → what would falsify it. §33 closes on five premises
argued *against* the survey's own thesis and six pre-registered concession conditions,
and the last sentence — *"be the kind of project that would notice if it were wrong"* —
lands because the preceding 80,000 words earned it.

**Three round-4 stylistic findings I can now report as fixed.** Doroudi is told **once**
in the whole paper (`survey/32` only), not three times. `## The nulls, given their own
space` is at 6, and I will stop counting it — six sections of thirty-three using a
consistent structural heading is a convention, not a tic. And the closers are no longer
a formula: reading all 33 end-to-end, the negation-antithesis figure is down to roughly
eight from eighteen, and the rest are genuinely different sentences (*"The slides are
just where you can see it"*; *"we cannot currently say whether the room would even be in
their language"*; *"Nobody's thumb, ever again"*). That was real editorial work.

**Three things still break the reading, none of them the argument.**

**1. `PAPER.md` is structurally flat, and the build already knows how to fix it.**
`evidence/build-paper.py:493` defines `_demote()`, whose docstring says: *"Section
bodies use `##` for their own subheads, which collides with the section heading level
once they are stitched into one document."* Exactly right. It is called **only on the
HTML path**. The markdown assembly at `:191` writes `f"\n## {n}. {title}\n{srcline}\n{text}\n"`
with `text` un-demoted, so `PAPER.md` contains **287 `##` headings**: 33 sections and
254 subsections at the same level, with numbering that restarts. `PAPER.md:151` is
`## 1. The Floor — …`; `PAPER.md:170` is `## 1. Three facts that govern every number in
this document`. On GitHub, which is where `README.md:9` sends the reader for *"as one
markdown file"*, those render identically. `docs/paper.html` is correct — 40 h2, 258 h3.
**Fix: `_demote(text)` at `:191`. One call. It also disambiguates half of the 23
cross-references above.**

**2. The abstract still describes half the paper.** Measured again, unchanged from round
4: Part III *The mechanisms* is **21.0%** of the words, Part IV *Correctness* **9.7%**,
Part VI *The field* **22.3%** — **53% of the paper, and none of the three appears in the
abstract**. Retrieval, spacing, teaching-to-learn, the ladder, grounding, assessment,
the canon, the market, thirty-five prior builds read as evidence: absent. Meanwhile the
compression paragraph (§6, 1,660 words, 2% of the paper) runs eight lines. Round 4's fix
item 5, not done. **Fix: two sentences for Part III with the numbers that already exist,
one for Part VI, and cut the compression paragraph to three lines.**

**3. The paper's own reading instructions describe a document this is not.**
`PAPER.md:70` — *"**Every claim carries an evidence label** — `MEASURED-RCT`,
`MEASURED-META`, `MEASURED-BENCH`, `OBSERVED`, `VENDOR`, `DEMO`, `INFERENCE`"* — repeated
at `README.md:128`. Counted across all 80,725 words: `MEASURED-RCT` **1**,
`MEASURED-META` **1**, `MEASURED-BENCH` **2**, `OBSERVED` **2**, `VENDOR` **7**, `DEMO`
**2**, `INFERENCE` **6** — **23 labels**, and **25 of the 33 sections contain none at
all**, including §3 The Scoreboard, the flagship evidence section. The labels are real
and they are rigorous — `research/raw/` carries **2,847** of them. They just did not
survive into the survey, which instead carries provenance in prose and in table columns
(*"Pre-registered RCT; 1,763 enrolled, 1,423 analysed; model swapped mid-trial"*), which
is a defensible choice. The *sentence* is the problem: a referee's first move on a
survey advertising evidence grading is to check the grading, and page 2's promise is
false of the body by a factor of a hundred. **Fix: one sentence — "Every claim states
its design, sample and source in the sentence or table that carries it; the underlying
research reports in `research/raw/` carry a formal label vocabulary — `MEASURED-RCT`,
… — and each section names its source report."** That is true, and it is a better
claim than the one being made.

---

## WHAT WOULD STILL EMBARRASS THE AUTHORS

Ranked. The first two are the only ones I would hold a launch for.

1. **Three different generated answers to "how many of our errors did the reviewer
   find?"** — 17 of 37, 16 of 37, 15 of 36, in the abstract, the README and the ledger
   respectively. On the document whose pitch is that the field does not measure what it
   claims to measure. Fourth round on this number. Cause and fix under C-35 above.
2. **The abstract contradicts §7 on the paper's own organising rule**, in the form the
   ledger records as corrected. `PAPER.md:27` vs `:1547`; `PAPER.md:30` vs `:1567`;
   `README.md:20`. And the checker cannot see it, because it has no rule past C-22.
3. **Nine local errors in the body — the first round in which I have found these.**
   Every one verified in the file. Ranked; the first three are the paper committing a
   sin it names elsewhere.

   a. **`PAPER.md:8327`, 128 notebooks from a scan of three.** Above. The denominator
      failure, in the section that grades other people's denominators.

   b. **`PAPER.md:7323`, r = 0.421 offered as proof of non-transfer.** Above.

   c. **`PAPER.md:4058–4070`, the Doroudi breakdown, which does not sum and then
      selectively reports itself.** *"**21 of 41 (51%)** significantly beat all
      baselines; 10 found no significant difference; 1 where the baseline won"* —
      21 + 10 + 1 = **32**, and nine of the 41 are unaccounted for. Then, twelve lines
      after the sentence *"Publishing the 0-of-8 sub-cut without the 21-of-41 headline
      is selective reporting, and it is the exact failure this survey exists to name"*,
      `:4070` says *"has **never** worked on material with structure. **Every trial
      that tried failed**"* and the section's binding commitment at `:4178` cites
      *"**0 of 8**"* and nothing else. **Fix: account for the nine; carry both numbers
      into `:4070` and `:4178`.**

   d. **`PAPER.md:3403` / `:3409` vs `:4582–4583` — one Welch statistic, two studies.**
      §14 gives *"three between-groups pre/post experiments with **166 pupils aged
      9–10**"* and *"Welch t(41.7) = 1.015, p = 0.316"*. §18 gives *"null in **both**
      randomised, fidelity-documented head-to-heads (**N = 350**, within-class
      randomisation, **Welch t = 1.015, p = 0.316**)"*. An identical *t* to three
      decimals cannot describe both, and `t(41.7)` implies ~44 in the contrast, which
      fits neither N. §18 is using one study's statistic to certify two.
      **Fix: give §18's N = 350 study its own statistic, or say plainly that it is
      re-citing the 166-pupil trial and drop *"both"*.**

   e. **`PAPER.md:5210` — SymPy's 38.3% is its own declaration of what it cannot do,
      presented as proof that it cannot tell you what it cannot do.** The L3 row reads
      *"SymPy **fails** 152/397 = 38.3% of the Wester suite"* and, in the same row,
      *"anything outside its competence, **and it cannot tell you when it is outside**"*.
      `F3:280` is explicit: **150 `@XFAIL` + 3 `@SKIP`**, counted by this project on
      2026-07-27 from `test_wester.py`. `PAPER.md:3908` gets it right — *"a **38.3%
      XFAIL/SKIP hole**"* — so the document says both things. And the same table flags
      its millisecond figures as this project's own measurements (`:5102`) while giving
      this one no provenance. **Fix: adopt §15's phrasing in §20, mark it internal, and
      strike the "cannot tell you" cell, because `@XFAIL` is that telling.**

   f. **`PAPER.md:7416` — *"notebooks reproduce their own stated results 4.03% of the
      time. **Two orders of magnitude better**"*** against 100%. 100 / 4.03 = **24.8×**.
      This is the **third** live misuse of *"two orders of magnitude"* for a figure
      between 24× and 30×; see also `PAPER.md:6016` and `:9691` (328 DI studies against
      11 AI studies = 29.8×, a studies-vs-studies presentation followed by *"Read the
      ratio"*) and `survey/04:71`. It has stopped being a slip and become a house habit.
      **Fix: state the ratio. "Twenty-five times better." "Thirty times more studies."**

   g. **`PAPER.md:3419` — the pull-quote says *"**Five** rungs exist so that different
      learners start at different points"*, under a heading that reads *"**Three** rungs,
      not five"* (`:3398`), two lines after *"what the **four** levels are for"*
      (`:3401`), and the section's own commitment at `:3668` says *"**Four** rungs exist
      as a library."* The design is ELI10/15/20/25 — four. Three counts on one page, and
      the wrong one is in the sentence set apart for emphasis. **Fix: `:3419` → "Four".**

   h. **`PAPER.md:4295–4297` — *"scaffolding showed a small **descriptive advantage**
      and no significant difference, **g = −0.08** [−0.20, 0.04]"*.** A negative *g*
      does not favour scaffolding. Either the word or the sign is wrong, and the
      commitment at `:4506` — *"**Do not scaffold the struggle phase.** g = −0.08"* —
      converts a straddling CI into a prohibition, which the paper refuses to do
      elsewhere (`:6140`, *"restraint removes harm and has **not been shown to teach**"*).
      **Fix: settle the direction; soften to "no difference demonstrated."**

   i. **`PAPER.md:7087` — *"a 500 ms silence timer plus 300 ms prefix padding … exceeds
      the human modal gap by **2.5–5×**"*** against the 100–200 ms gap stated at `:7041`.
      800 / 200 = 4; 800 / 100 = 8. **Fifth round on this one.**

4. **Twenty-three cross-references that land on a real, wrong section**, three of them
   in a bolded build order. Worse per instance than the dead links they replaced. Plus
   two that resolve correctly to sections that do not contain the figure being cited.
4. **`PAPER.md` has 287 undifferentiated `##` headings** because `_demote()` is called
   on one of the two output paths.
5. **`−3.20pp` / `+8.14pp` stated twice as measurement and once as a bolded
   commitment**, when `K2` labels the pair `INTERNAL-PRIOR` — the project citing itself
   as external evidence, in the section arguing that the field optimises against
   instruments that do not work. Third round.
6. **`PAPER.md:1629`, the 93.8% render rate quoted as a working pipeline**, pointed at
   §19, whose thesis is that a picture's success rate is not its correctness. Overall
   score 0.77 and the authors' layout caveat both dropped. Third round.
7. **Page 2 promises an evidence-label scheme the body does not use.**
8. **Carry-over, unfixed, and I will list them once more without argument.**
   `survey/16:57–59` *"exceeds the human modal gap by 2.5–5×"* — 800 ms against
   100–200 ms is 4–8×; **fifth round**.
   `survey/23:204` *"**Publish the denominator.** '1,565 papers' without '7 RCTs' is a
   misleading sentence"* — 1,565 *is* the denominator; the commitment inverts its own
   terms, in bold; **fifth round**, and it is also `PAPER.md`'s closing line for §4.
   `survey/20:285` *"two orders of magnitude"* for 30× — **fifth round**, and it is in
   §33's concession paragraph, the most carefully-read page in the paper.
   `survey/04:217` states §300.624 destruction as unconditional inside a list headed
   *"Four hard limits, and they are not negotiable"*, against `H2:2852–2855`'s
   §300.624(b) permanent-record carve-out — **third round**, and it is the one legal
   overclaim in a document that is otherwise careful about law.
   `survey/29:219` *"with zero false alarms"*, unconditional — **fourth round**.
   `survey/30:81` *"a **median 49 minutes against 60**"* without the *assumed* qualifier
   that `survey/03:52` carries — **fourth round**.
   `survey/01:21–28` — the *"These are not incremental — but…"* stump and the bolded
   *"**Corrected in place:**"* build artifact, verified fixed in round 3, regressed in
   round 4, **still regressed**. It is the only fix in five rounds that has been undone.

---

## WHAT IS ACTUALLY GOOD

Shorter than last time, because most of what I would have listed I already listed. New,
and I would defend each line:

- **§7's correction to its own rule is better than the correction I asked for.** I asked
  for one weaker verb. What arrived was the verb, the restored table row, and four
  sentences explaining *why* a bound and not an equality — *"A weak check bounds you low.
  A strong one does not guarantee you reach the bound; it only stops you being capped
  below it."* A rule that has met its counter-example in print and survived is worth more
  than one that has not, and this one now has.
- **§23 §1.** *"Every new teacher, every schedule change, and every substitute a
  re-notification event."* That is §300.323(d)(2) turned into a week, and it is the kind
  of sentence that makes a practitioner trust the rest of a document. The section that
  was a costume in round 3 and a perimeter in round 4 is now a job.
- **The cross-reference pass itself.** `renumber()` runs in a first pass, before any
  prose is rendered, off the same declarative outline that generates the contents, and
  reports what it could not resolve. The architecture is right; it is one regex short of
  complete. C-31, C-34 and the contents links were all diagnosed as the same defect —
  two schemes, no single source — and all three were fixed by giving the build one
  source. That is the correct instinct and it is unusual.
- **The 223-domain result, still.** *"No LLM beat chance at labelling an incorrect
  student action."* One measurement that earns a whole survey's modesty.
- **§33's concession conditions.** Six, numbered, pre-registered, with the honest
  observation that *"if (1) and (6) both land, the correct revision is not a hedge"* and
  a fully-written alternative thesis. Almost nobody does this. It is the single strongest
  reason to believe the rest of the document.
- **`CORRECTIONS.md`, still the best artifact here** — even given that its scoreboard
  drops a row. Thirty-seven rows, append-only, provenance on every one, and C-36 and
  C-37 in it are a project logging, in its own ledger, that its checker was defeated
  seventeen ways and its headline rule was overstated. I have not seen another survey
  ship one, and it is why this review could be specific rather than impressionistic.

---

## THE PROOF-STAGE LIST

Everything below is a build or an edit. None needs research. 1–4 before anyone reads it.

1. `build-paper.py:65` — delete `N_EXT`; use the `:611` computation. `sync-ledger.py` —
   fix the row regex and assert row count against a simple `^\| \*\*C-\d+\*\*` count.
   Re-run both. Correct: **16 of 37**.
2. `build-paper.py:27,30` and `README.md:20,23–24` — *equals* → *is bounded by*; move
   SciCode out of *"where none exists"*.
3. `build-paper.py:191` — `_demote(text)`.
4. `renumber()` — rewrite single-digit `§N` and `§N.M` to `§<paper>.N`; report them.
5. `PAPER.md:1566` — one sentence conceding the difficulty confound. `:1600`, `:1712` —
   label the −3.20pp/+8.14pp pair `INTERNAL-PRIOR`. `:1629` — the 0.77 overall score and
   the layout caveat.
6. `check-corrections.py` — fail on any ledger id with neither a rule nor a written
   waiver; per-branch probes; Unicode normalisation; `--self-test` always scans; assert
   every literal surface exists; call it from `.autosave.sh` and refuse to push on
   non-zero.
7. `survey/31` — the 16.8-against-4 caseload arithmetic from `H2:1375–1394`; the SPeNSE
   2002 vintage; rewrite `:253`.
8. `PAPER.md:70` / `README.md:128` — describe the labelling the paper actually does.
9. Abstract: two sentences for Part III, one for Part VI, cut the compression paragraph
   to three lines. Fix `:19–20`'s double space.
10. `README.md:6,141`, `docs/index.html:106` — *"~2,100 sources"* → generated or deleted.
    `README.md:113` — *"three review reports"* → four.
11. The seven carry-overs in item 8 of the previous section.

---

*Reviewed at `e1ed691`. Every quotation was read in the file it is attributed to; every
count was recomputed with `glob`, `grep` and a parser written for this review; the
checker was attacked on three copies under
`/tmp/claude-1000/-home-eranti/…/scratchpad/` and the working tree was not modified.*
